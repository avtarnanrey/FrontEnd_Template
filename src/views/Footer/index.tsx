import * as React from "react";

interface ComponentProps {
}

export const Footer = (props: ComponentProps) => {
    const { } = props;

    return (
        <div>
            Footer
        </div>
    );
};